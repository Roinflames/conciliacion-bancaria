name: Notify Deploy

on:
  push:
    branches: [main, master, develop]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Notify deploy result
        if: always()
        run: |
          STATUS="success"
          if [ "${{ job.status }}" != "success" ]; then STATUS="failed"; fi

          curl -s -X POST "${{ secrets.DEPLOY_NOTIFY_URL }}/deploy-events" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.DEPLOY_NOTIFY_TOKEN }}" \
            -d "{
              \"app\":            \"conciliacion-bancaria\",
              \"env\":            \"${{ github.ref_name }}\",
              \"version\":        \"${{ github.ref_name }}\",
              \"status\":         \"$STATUS\",
              \"commit_sha\":     \"${{ github.sha }}\",
              \"commit_message\": \"${{ github.event.head_commit.message }}\",
              \"author\":         \"${{ github.actor }}\",
              \"commit_url\":     \"https://github.com/${{ github.repository }}/commit/${{ github.sha }}\"
            }"
# deploy-notifier test mié 25 feb 2026 22:07:46 -03
# kong test jue 26 feb 2026 00:32:12 -03
